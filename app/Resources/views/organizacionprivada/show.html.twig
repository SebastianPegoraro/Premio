{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title main-title">Organización Privada: {{ organizacionPrivada }}</h3>
                </div>
                <div class="panel-body">
                    {% include ':organizacionprivada:show-info.html.twig' %}
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
                            <ul class="list-inline">
        <li>
            <a class="btn btn-default" href="{{ path('organizacionprivada_index') }}"><i class="fa fa-chevron-left"></i> Volver a la Lista</a>
        </li>
        <li>
            <a class="btn btn-info" href="{{ path('organizacionprivada_pdf', { 'id': organizacionPrivada.id }) }}"><i class="fa fa-file-pdf-o"></i> PDF</a>
        </li>
        <li>
            <a class="btn btn-success" href="{{ path('organizacionprivada_edit', { 'id': organizacionPrivada.id }) }}"><i class="fa fa-pencil"></i> Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <button class="btn btn-danger" type="submit" value="Delete"><i class="fa fa-trash"></i> Eliminar</button>
            {{ form_end(delete_form) }}
        </li>
    </ul>
</div>
    </div>

    </div>
{% endblock %}
