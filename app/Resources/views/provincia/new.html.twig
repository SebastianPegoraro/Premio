{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
	<h1>Creación de Provincia</h1>
	<div class="row">
		<div class="col-sm-12">
	    {{ form_start(form, { "attr": {"class": "well"} }) }}
	        {{ form_widget(form) }}
	        <div class="text-right">
	        	<button class="btn btn-primary" type="submit" value="Create" >
		        	<i class="fa fa-plus"></i> Crear
		        </button>
	        </div>
	    {{ form_end(form) }}

				    <ul class="list-inline">
        <li>
            <a class="btn btn-default" href="{{ path('admin_provincia_index') }}"><i class="fa fa-chevron-left"></i> Volver a la Lista</a>
        </li>
    </ul>
</div>
	</div>
</div>
{% endblock %}
