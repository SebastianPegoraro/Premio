{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		{{ dump(form.dateFilter) }}
		{{ form_start(form) }}
			{{ form_widget(form) }}
			<div class="text-right">
				<button class="btn btn-primary">Go!!</button>
			</div>
		{{ form_end(form) }}
	</div>
{% endblock %}

{% block page_javascripts %}
	{{ form_javascript(form.dateFilter) }}
{% endblock %}